{
  "name": "chainsage-contracts",
  "version": "1.0.0",
  "description": "ChainSage Smart Contracts for 0G Mainnet",
  "scripts": {
    "deploy:testnet": "hardhat run scripts/deploy.js --network 0g-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network 0g-mainnet",
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
